# Database — MySQL 8+
# Format: user:password@tcp(host:port)/dbname?parseTime=true
DB_DSN=
# Asgardeo JWT validation
AUTH_JWKS_ENDPOINT=
AUTH_ISSUER=
AUTH_AUDIENCE=
# Set to false to skip signature verification in local development
# AUTH_TOKEN_VALIDATOR_ENABLED=true
AUTH_TOKEN_VALIDATOR_ENABLED=false


# Azure Blob Storage (risk and audit evidence files)
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_STORAGE_CONTAINER=

# Server
PORT=:8080
