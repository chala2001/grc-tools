# Frontend environment variables
# Copy this file to .env.local and fill in your values.

# Asgardeo OAuth application (build-time). Optional — sensible defaults are
# baked into src/main.tsx; override here to point at a different tenant/app.
VITE_ASGARDEO_CLIENT_ID=
VITE_ASGARDEO_BASE_URL=
